<div class="row">
    {{ errorMessage }}
    <div class="col-md-6">
    {{ form('admin/index/add', 'method': 'post') }}
        <fieldset>
            <div class="form-group">
                <label for="name">Name</label>
                <div>
                    {{ text_field('name', 'class': 'form-control') }}
                </div>
            </div> 
            <div class="form-group">
                {{ submit_button('Submit', 'class': 'btn btn-primary') }}
            </div>
        </fieldset>
    </form>
    </div>
    <div class="col-md-6"> 
        {{link_to('/admin/index/index', 'List')}}
    </div>
</div>